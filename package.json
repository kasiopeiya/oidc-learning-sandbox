{
  "name": "oidc-learning-sandbox",
  "version": "1.0.0",
  "private": true,
  "description": "OIDC認可コードフロー学習サンドボックス",
  "scripts": {
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.0",
    "lint-staged": "^15.2.0",
    "prettier": "^3.4.0"
  },
  "lint-staged": {
    "*.{ts,tsx}": "prettier --write",
    "*.{json,md}": "prettier --write"
  }
}
