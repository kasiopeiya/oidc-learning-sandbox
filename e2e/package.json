{
  "name": "oidc-learning-sandbox-e2e",
  "version": "1.0.0",
  "description": "E2E tests for OIDC learning sandbox",
  "type": "module",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "load-env": "source scripts/load-env.sh"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0",
    "@aws-sdk/client-cognito-identity-provider": "^3.400.0",
    "@types/node": "^20.10.0",
    "typescript": "^5.3.0"
  }
}
